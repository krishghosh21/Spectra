# Spectra
A fast, colourful terminal toolkit to inspect and export Instagram profile data via the mobile API. View profile details, posts, followers and following, run integrity checks against reported counts, and save everything as JSON with media in one command. Truecolor UI with gradients, spinners and live progress bars. Powered by instagrapi.
